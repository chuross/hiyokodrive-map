services:
  maputnik:
    image: maputnik/editor
    platform: linux/amd64
    ports:
      - "3000:80"
  
  file-server:
    image: halverneus/static-file-server
    ports:
      - "3001:8080"
    volumes:
      - ./pmtiles:/web
    environment:
      - CORS=true
